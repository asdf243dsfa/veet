{
	"name": "intellimeet",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler pages deploy",
		"dev": "echo '🚀 Starting Intellimeet on http://localhost:8788' && wrangler pages dev",
		"start": "wrangler pages dev",
		"test": "vitest",
		"translate": "node translate.js"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.1.0",
		"vitest": "1.3.0",
		"wrangler": "^3.0.0"
	}
}
